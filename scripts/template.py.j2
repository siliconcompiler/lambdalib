from siliconcompiler import DesignSchema
from lambdalib._common import basic_setup

class {{ class_name }}(DesignSchema):
    def __init__(self):
        name = '{{ module_name }}'
        super().__init__(name)
        basic_setup(self, __file__, name)

if __name__ == "__main__":
    d = {{ class_name }}()
    d.write_fileset(f"{d.name()}.f", fileset="rtl")
